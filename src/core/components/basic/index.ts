import { withInstall } from '@/utils'
import basicArrow from './children/basic-arrow.vue'
import basicTitle from './children/basic-title.vue'
import basicHelp from './children/basic-help.vue'

export const BasicArrow = withInstall(basicArrow)
export const BasicTitle = withInstall(basicTitle)
export const BasicHelp = withInstall(basicHelp)
